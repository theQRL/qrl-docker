# Keep the build context tiny — everything the Dockerfile needs is fetched
# inside the build. Critically, this keeps backup/ (wallet + chain state) out
# of the context and therefore out of the image.
*

!entrypoint.sh
